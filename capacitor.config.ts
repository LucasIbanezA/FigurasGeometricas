import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.calcularperimetros',
  appName: 'calcularperimetros',
  webDir: 'www'
};

export default config;
